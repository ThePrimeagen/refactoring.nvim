class Poggers:

    def foo(self, a):
        for x in range(self.test_other + self.test):
            print(x, a)


    def simple_function(self, a):
        self.test = 1
        self.test_other = 11
        self.foo(a)
