class Poggers:

    def foo(self, a, test):
        for x in range(self.test_other + self.test + test):
            print(x, a)


    def simple_function(self, a):
        test = 3
        self.test = 1
        self.test_other = 11
        self.foo(a, test)
